# Features docs moved

Feature pages now live in the public docs site:

**[`docs-site/features/`](../../docs-site/features/)**

Local preview:

```bash
cd docs-site
python3 -m http.server 8765
# http://127.0.0.1:8765/features/
```

External deploy URLs are not wired in this anonymous build.
