# Features docs (`docs-site/features`)

Архитектура и каталог локальных фич ResearcherOS. Публикуется вместе с docs-site.

## Локально

```bash
cd docs-site
python3 -m http.server 8765
# http://127.0.0.1:8765/features/
```
